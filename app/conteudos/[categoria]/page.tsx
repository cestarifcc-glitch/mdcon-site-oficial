import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { contentCategories, getCategory } from '../content-data';
export function generateStaticParams(){return contentCategories.map(c=>({categoria:c.slug}))}
export async function generateMetadata({params}:{params:Promise<{categoria:string}>}):Promise<Metadata>{const {categoria}=await params;const c=getCategory(categoria);if(!c)return {};return{title:`${c.title} | Conteúdos MDCon`,description:c.description}}
export default async function CategoryPage({params}:{params:Promise<{categoria:string}>}){const {categoria}=await params;const c=getCategory(categoria);if(!c)notFound();return <main className="cc"><style>{`.cc{background:#fff;color:#161616}.w{width:min(980px,calc(100% - 40px));margin:auto}.hero{padding:58px 0 45px;border-bottom:1px solid #eee;background:#fbfbf8}.back{color:#777;text-decoration:none;font-weight:700}.ey{display:block;margin-top:24px;color:#FFC400;font-size:12px;font-weight:900;letter-spacing:1.4px}.hero h1{max-width:780px;margin:12px 0 16px;font-size:clamp(38px,4vw,52px);line-height:1.05;letter-spacing:-2px}.hero p{max-width:730px;color:#606060;font-size:17px;line-height:1.7}.list{padding:48px 0 70px}.card{display:block;padding:26px 0;border-bottom:1px solid #e8e8e4;color:#161616;text-decoration:none}.card:first-child{border-top:1px solid #e8e8e4}.num{color:#FFC400;font-size:12px;font-weight:900}.card h2{margin:8px 0 9px;font-size:25px}.card p{margin:0 0 12px;color:#666;line-height:1.65}.more{font-weight:850;color:#FFC400}@media(max-width:620px){.w{width:calc(100% - 28px)}.hero{padding:40px 0 34px}.hero h1{font-size:35px}.list{padding:36px 0 55px}}`}</style><section className="hero"><div className="w"><Link className="back" href="/conteudos">← Conteúdos</Link><span className="ey">MDCON • CONTEÚDOS</span><h1>{c.title}</h1><p>{c.description}</p></div></section><section className="list"><div className="w">{c.articles.map((a,i)=><Link className="card" href={`/conteudos/${c.slug}/${a.slug}`} key={a.slug}><span className="num">0{i+1}</span><h2>{a.title}</h2><p>{a.summary}</p><span className="more">Ler conteúdo →</span></Link>)}</div></section></main>}
